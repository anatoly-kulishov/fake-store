export enum AppRoutesEnum {
  HOME = '/',
  USERS = '/users',
  PRODUCTS = '/products',
  PRODUCTS_CATEGORY = '/products/category',
  AUTH = '/auth',
  UIKIT = '/ui-kit',
  SEARCH = '/search',
}

export enum AppRouteKeys {
  HOME = 'Home',
  USERS = 'Users',
  PRODUCTS = 'Products',
  AUTH = 'Auth',
  UIKIT = 'UI Kit',
  SEARCH = 'Search',
}
