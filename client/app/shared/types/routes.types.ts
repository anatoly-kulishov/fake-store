export enum AppRoutesEnum {
  HOME = '/',
  USERS = '/users',
  PRODUCTS = '/products',
  AUTH = '/auth',
  UIKIT = '/ui-kit',
  SEARCH = '/search',
}

export enum AppRouteKeys {
  HOME = 'Home',
  USERS = 'Users',
  PRODUCTS = 'Products',
  AUTH = 'Auth',
  UIKIT = 'UI Kit',
  SEARCH = 'Search',
}
