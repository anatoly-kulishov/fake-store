export enum AppRoutesEnum {
  HOME = '/',
  USERS = '/users',
  PRODUCTS = '/products',
  AUTH = '/auth',
}

export enum AppRouteKeys {
  HOME = 'Home',
  USERS = 'Users',
  PRODUCTS = 'Products',
  AUTH = 'Auth',
}
